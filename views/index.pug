form(action="/todos")
  input(type="text",name="q")
  button Search
ul
  each todo in todos
    li
      span= todo.text
      span &nbsp 
      a(href="/todos/delete/" + todo.id) Delete
    
form(action="/todos/create", method="POST")
  input(type="text",name="text")
  button Create